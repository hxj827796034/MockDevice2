pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 使用专门针对 LSPosed 的 API 仓库
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MockDevice"
include ':app'
